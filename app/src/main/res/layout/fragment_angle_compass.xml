<?xml version="1.0" encoding="utf-8"?>
<!--
  角度计和指南针模拟页。
  行由代码按共用模板（item_value_row.xml）插入 —— 加一项只需在 Fragment 的 rows 列表里加一条。
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <com.google.android.material.textview.MaterialTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="角度计和指南针模拟"
                android:textColor="?attr/colorOnSurfaceVariant" />

            <com.google.android.material.textview.MaterialTextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/angle_page_desc"
                android:textSize="11sp" />

            <!-- 只读读数区：注入开关 + 三路噪声档现值 + 当前朝向。
                 改值不在本页（噪声档在 Calibration、开关在 Setting）—— 用户口径：
                 本域在 Setting 页没有设置项，噪声不搬。 -->
            <!-- 外周传感器模拟（本页那一侧）：开关即下发，不等下次握手 -->
            <com.google.android.material.switchmaterial.SwitchMaterial
                android:id="@+id/orientation_mock_switch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="@string/orientation_mock" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/angle_readout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:textSize="13sp" />

            <com.google.android.material.textview.MaterialTextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@string/angle_footer"
                android:textSize="11sp"
                android:textColor="?attr/colorOnSurfaceVariant" />

        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
