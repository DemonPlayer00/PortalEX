<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_main"
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--
      抽屉：**一个** NavigationView / **一个**菜单（列表只能有一个，否则会变成两个各自滚动的列表）。
      工具页（校准/诊断）排在菜单最后，两组之间的细分隔线由 `DrawerGroupDivider`（ItemDecoration）
      画在指定菜单项的上方 —— 菜单 XML 没有"组间距"这种属性。
    -->
    <!--
      抽屉 = **固定的标题区** + **一个**可滚动菜单列表：
      · 标题区（nav_header_main）不放进列表里 ⇒ 列表滚动时它不动（放进 `app:headerLayout`
        会成为列表的第一项，跟着一起滚）；
      · 列表只有一个 ⇒ 不会出现"两个各自滚动的列表"；
      · 列表用 `0dp + weight=1` 吃掉剩余高度并在内部滚动。
      两组菜单之间的细分隔线（含上下 margin）由 `DrawerGroupDivider` 画在指定菜单项上方。
    -->
    <!--
      ⚠️ 圆角只靠 background 是不够的：里面两层的**矩形背景会把圆角盖掉**
      （头部是 side_nav_bar 渐变、列表是 NavigationView 自己的 surface 背景）。
      所以这里是"圆角背景 + clipToOutline"两条一起上：clipToOutline 让子视图按背景的
      轮廓裁剪（GradientDrawable 的圆角会进 outline），于是头部的渐变也跟着圆角走。
      NavigationView 自己的背景置空，免得它在圆角外面又画一层直角底色。
    -->
    <LinearLayout
        android:id="@+id/drawer_container"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:orientation="vertical"
        android:background="@drawable/bg_drawer"
        android:clipToOutline="true">

        <include layout="@layout/nav_header_main" />

        <com.google.android.material.navigation.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            app:menu="@menu/activity_main_drawer" />

    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>